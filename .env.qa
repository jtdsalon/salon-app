# QA (mode=qa). Override with .env.local if needed.
# Required: API base URL (all API calls and image origin). No default in code.
# Use full URL or /api if same-origin proxy.
VITE_APP_BASE_URL=http://129.212.226.33/api
# Optional: CSP img-src origins (uploads served from same host).
# VITE_CSP_IMG_SRC=http://129.212.226.33

VITE_APP_API_KEY=
