/**
 * Salon-app theme color constants.
 * Use these instead of inline hex values for consistency and easy updates.
 */
export const ACCENT_COLOR = '#E2C275';
export const ACCENT_COLOR_DARK = '#C9A84D';
/** For box-shadow, gradients, etc. RGB of ACCENT_COLOR. */
export const ACCENT_COLOR_RGBA = (alpha: number) => `rgba(226, 194, 117, ${alpha})`;
